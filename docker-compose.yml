version: '3.8'

services:
  mongo:
    image: mongo:5.0
    container_name: my-mongo
    ports:
      - "27017:27017"
    # volumes:
    #   - ./data/db:/data/db
    # environment:
    #   MONGO_INITDB_ROOT_USERNAME: root
    #   MONGO_INITDB_ROOT_PASSWORD: example
